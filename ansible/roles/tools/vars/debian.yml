---
# Five of the macOS tools have no apt package and no cargo, PyPI or npm
# equivalent either — actionlint and pinact are Go binaries released only as
# archives, rtk ships via Homebrew alone, and ollama wants its own installer
# script. They stay macOS-only until someone adds a download-and-extract task;
# `just diff` on an Ubuntu box reports them rather than letting them go quiet.
apt_tools:
    - asciinema
    - bat
    - bats
    - build-essential
    - clang-format
    - containerd.io
    - cppcheck
    - docker-buildx-plugin
    - docker-ce
    - docker-ce-cli
    - docker-ce-rootless-extras
    - docker-compose-plugin
    - dotnet-sdk-10.0
    - eza
    - fd-find
    - ffmpeg
    - flatpak
    - fzf
    - gh
    - ghostscript
    - git
    - git-delta
    - git-filter-repo
    - git-lfs
    - gnome-software-plugin-flatpak
    - gnupg
    - graphviz
    - hyperfine
    - imagemagick
    - jq
    - just
    - less
    - libimage-exiftool-perl
    - libpq-dev
    - lld
    - miller
    - p7zip-full
    - pandoc
    - pkg-config
    - poppler-utils
    - powershell
    - python3
    - python3-pip
    - ripgrep
    - shellcheck
    - shfmt
    - upx-ucl
    - wabt
    - yq
    - zoxide
    - zsh
    - zsh-autosuggestions
    - zsh-syntax-highlighting

snap_tools:
    - name: lazygit
      classic: false
    - name: lefthook
      classic: true

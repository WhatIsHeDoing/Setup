---
# Git configuration applied on all platforms
git_config:
    core.excludesFile: "~/.gitignore_global"
    core.pager: "delta"
    delta.navigate: "true"
    delta.dark: "true"
    diff.colorMoved: "default"
    fetch.prune: "true"
    init.defaultBranch: "main"
    interactive.diffFilter: "delta --color-only"
    merge.conflictstyle: "zdiff3"
    push.autoSetupRemote: "true"
    push.default: "current"

# Cargo packages installed on all platforms
cargo_packages:
    - cargo-modules
    - cargo-outdated
    - cargo-update
    - diskonaut
    - wasm-pack

# Python packages installed on all platforms
pip_packages:
    - ansible-core
    - ansible-lint
    - checkov
    - ipykernel
    - pip
    - pre-commit
    - setuptools

# pnpm packages installed globally on all platforms
pnpm_packages:
    - cspell

# VS Code extensions installed on all platforms
vscode_extensions:
    - davidanson.vscode-markdownlint
    - eamodio.gitlens
    - editorconfig.editorconfig
    - sdras.night-owl
    - usernamehw.errorlens
    - vscode-icons-team.vscode-icons

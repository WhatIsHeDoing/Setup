[defaults]
roles_path            = ansible/roles
collections_path      = ./collections:~/.ansible/collections
display_skipped_hosts = False
# Only the facts the playbooks read; a new fact use must extend this subset.
gather_subset         = !all,!min,distribution,env,platform
inject_facts_as_vars  = True
result_format         = yaml
callbacks_enabled     = ansible.builtin.timer, ansible.posix.profile_tasks, community.general.selective
# The Touch ID dialog outlives the 10s become default.
timeout               = 30
